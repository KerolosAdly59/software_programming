#include <stdio.h>
int main()
{
    //waild pointer
    // int *ptr ; 
    // int *ptr = NULL ;
    printf("%p\n",ptr);
    printf("%d",*ptr);
    swap();
    int *ptr = x ; 
}

void swap(int *x,int*y)
{
    int x  =10 ; 
    
return &x
}